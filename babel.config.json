{
  "presets": [
    [
      "@babel/preset-env",
      {
        "targets": { "node": "current" }
      }
    ]
  ],
  "use": { "loader": "babel-loader", "options": { "presets": ["es2015", "stage-0"]}}
}
